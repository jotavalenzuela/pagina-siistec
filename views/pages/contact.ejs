<!-- views/pages/contact.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script>// <![CDATA[
      $(document).ready(function(){
        var from,to,subject,text;
        $("#send_email").cilck(function(){
          to=$("#to").val();
          subject=$("#subject").val();
          text=$("content").val();
          $("#message").text("Enviando Mail... Favor espere");
          $.get("http://localhost:8080/send",{to:to,subject:subject,text:text},function(data){
          if(data=="sent")
          {
            $("message").empty().html("El mail se envio a "+to+" para ser revisado, por favor espere a que nos pongamos en contacto con ud.");
          }
          });
            });
    }));
    </script>
</head>
<body class="container">

    <header>
        <link rel="stylesheet" type="text/css" href="../bootstrap.min.css">
        <% include ../partials/header %>
    </header>

    <main>
    <div class="jumbotron">
      <form class="form-horizontal">
      <fieldset>
        <legend>Envianos tus dudas</legend>
        <div class="form-group">
          <label for="inputEmail" class="col-lg-2 control-label">Email</label>
          <div class="col-lg-10">
            <input type="text" class="form-control" id="to" placeholder="Tu e-mail">
          </div>
        </div>
        <div class="form-group">
          <label for="inputPassword" class="col-lg-2 control-label">Tema</label>
          <div class="col-lg-10">
            <input type="text" class="form-control" id="subject" placeholder="Tema de la consulta">
          </div>
        </div>
        <div class="form-group">
          <label for="textArea" class="col-lg-2 control-label">Escribe aquí tus dudas</label>
          <div class="col-lg-10">
            <textarea class="form-control" rows="3" cols="40"  id="textArea"></textarea>
            <span class="help-block">LIMITAR A 120 caracteres.</span>
          </div>
        </div>
        <div class="form-group">
          <div class="col-lg-10 col-lg-offset-2">
            <button type="reset" class="btn btn-default">Cancelar</button>
            <button type="submit" class="btn btn-primary">Enviar</button>
          </div>
        </div>
  </div>
<!--agregar ALERTAS!!!!!!!!!!!!!!!!!!!!-->
    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>

</body>
</html>
